Public Class DefaultSettings
    'Campos por defecto si no se introduce nada en los dos primeros textbos (se dejan en blanco, Server y database, cogera estos por defecto)
    Public Shared serverOr As String = "(local)"
    Public Shared dataBaseOr As String = "DB"
End Class
